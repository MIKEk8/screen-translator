<Window x:Class="ScreenTranslator.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Screen Translator"
        Height="600" Width="960"
        MinHeight="400" MinWidth="700"
        Background="{StaticResource BgPrimaryBrush}"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border CornerRadius="16" Background="{StaticResource BgPrimaryBrush}"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource BgSecondaryBrush}"
                    CornerRadius="16,16,0,0" Padding="16,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image x:Name="TitleBarIcon" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <TextBlock Text="Screen Translator" FontSize="14" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="C#" FontSize="10" Foreground="{StaticResource AccentBrush}"
                                   VerticalAlignment="Center" Margin="8,0,0,2" FontWeight="Bold"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Content="&#x2014;" Click="Minimize_Click"
                                Style="{StaticResource AccentButton}" Padding="8,4" FontSize="12" Margin="0,0,4,0"/>
                        <Button Content="&#x2716;" Click="Close_Click"
                                Style="{StaticResource AccentButton}" Padding="8,4" FontSize="12"
                                Background="#e74c3c"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Body: Sidebar + Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="56"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{StaticResource BgSecondaryBrush}"
                        CornerRadius="0,0,0,16" Padding="6,12,6,6">
                    <DockPanel LastChildFill="False">
                        <StackPanel DockPanel.Dock="Top">
                            <Button x:Name="NavTranslate" Content="&#x1F4DD;"
                                    ToolTip="Translate"
                                    Style="{StaticResource NavButtonActive}"
                                    Click="NavTranslate_Click"
                                    HorizontalContentAlignment="Center"
                                    Padding="0,10" FontSize="20" Margin="0,0,0,4"/>
                            <Button x:Name="NavPreview" Content="&#x1F5BC;"
                                    ToolTip="Capture Preview"
                                    Style="{StaticResource NavButton}"
                                    Click="NavPreview_Click"
                                    HorizontalContentAlignment="Center"
                                    Padding="0,10" FontSize="20" Margin="0,0,0,4"
                                    Visibility="Collapsed"/>
                            <Button x:Name="NavSettings" Content="&#x2699;"
                                    ToolTip="Settings"
                                    Style="{StaticResource NavButton}"
                                    Click="NavSettings_Click"
                                    HorizontalContentAlignment="Center"
                                    Padding="0,10" FontSize="20" Margin="0,0,0,4"/>
                            <Button x:Name="NavAbout"
                                    ToolTip="About"
                                    Style="{StaticResource NavButton}"
                                    Click="NavAbout_Click"
                                    HorizontalContentAlignment="Center"
                                    Padding="0,10" FontSize="20">
                                <Grid>
                                    <TextBlock Text="&#x2139;" FontSize="20" HorizontalAlignment="Center"/>
                                    <Ellipse x:Name="UpdateBadge" Width="8" Height="8"
                                             Fill="#4ade80" HorizontalAlignment="Right" VerticalAlignment="Top"
                                             Margin="0,-2,-2,0" Visibility="Collapsed"/>
                                </Grid>
                            </Button>
                        </StackPanel>
                        <Button x:Name="NavPause" Content="&#x23F8;"
                                ToolTip="Pause hotkeys"
                                DockPanel.Dock="Bottom"
                                Style="{StaticResource NavButton}"
                                Click="NavPause_Click"
                                HorizontalContentAlignment="Center"
                                Padding="0,10" FontSize="20" Margin="0,4,0,0"/>
                    </DockPanel>
                </Border>

                <!-- Content Frame -->
                <Frame Grid.Column="1" x:Name="ContentFrame"
                       NavigationUIVisibility="Hidden"
                       Background="Transparent"/>
            </Grid>
        </Grid>
    </Border>
</Window>
